#include "Settings.hpp"

Settings::Settings()
    : screenWidth(1200),
      screenHeight(800),
      bgColor(sf::Color::Black),
      shipSpeed(10),
      bulletSpeed(5),
      bulletWidth(3),
      bulletHeight(12),
      bulletColor(sf::Color::White),
      bulletMaxCount(5)
{
}
